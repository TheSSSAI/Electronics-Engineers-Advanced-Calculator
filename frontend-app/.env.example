# ===================================================================
# == Application Environment Variables
# ==
# == IMPORTANT:
# == - DO NOT commit the .env file to version control.
# == - This file (.env.example) SHOULD be committed.
# == - All variables used by Vite MUST be prefixed with VITE_.
# ===================================================================

# --- API Configuration ---
# The base URL for the backend API Gateway.
# Example: https://api.yourdomain.com
VITE_API_BASE_URL=https://api.example.com

# --- AWS Cognito Configuration ---
# These values are required for the OAuth 2.0 PKCE authentication flow.
# They are obtained from the AWS Cognito User Pool configuration.

# The ID of the Cognito User Pool.
# Example: us-east-1_xxxxxxxx
VITE_COGNITO_USER_POOL_ID=

# The Client ID of the App Client configured in the User Pool.
VITE_COGNITO_CLIENT_ID=

# The AWS region where the Cognito User Pool is located.
# Example: us-east-1
VITE_COGNITO_REGION=

# The domain for the Cognito Hosted UI.
# This could be a Cognito domain or a custom domain.
# Example: your-app.auth.us-east-1.amazoncognito.com
VITE_COGNITO_DOMAIN=

# The full URL where Cognito will redirect the user after a successful login.
# This MUST match one of the allowed callback URLs in the App Client settings.
# Example: http://localhost:3000/auth/callback
VITE_COGNITO_REDIRECT_URI=

# The full URL where Cognito will redirect the user after a logout.
# This MUST match one of the allowed sign-out URLs in the App Client settings.
# Example: http://localhost:3000/
VITE_COGNITO_LOGOUT_URI=