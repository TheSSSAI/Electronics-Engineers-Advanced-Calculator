[{'name': 'Request-Reply', 'type': 'RequestReply', 'implementation': "Synchronous HTTP/REST communication between the Client SPA, Amazon API Gateway, and the backend services ('User & Data Service' and 'Formula Execution Service').", 'applicability': 'The fundamental pattern for all client-server interactions as specified by REQ-1-036 and REQ-1-039. It is used for all operations where the client requires an immediate response, such as fetching user data, managing custom modes, and executing formulas.'}, {'name': 'API Gateway', 'type': 'APIGateway', 'implementation': 'Amazon API Gateway is used as the single, managed entry point for the frontend client, routing requests to the appropriate backend service as defined in the architecture.', 'applicability': 'Decouples the client from the backend microservices. Essential for centralizing cross-cutting concerns like request routing, JWT validation (REQ-1-040, REQ-1-103), rate limiting, and generating Correlation IDs for distributed logging (REQ-1-067).'}, {'name': 'Retry', 'type': 'Reliability', 'implementation': 'A retry mechanism with exponential backoff should be implemented in the client-side API service for idempotent GET requests and potentially within the API Gateway configuration for backend integrations.', 'applicability': 'Essential for improving application resilience by transparently handling transient network errors or temporary service unavailability, preventing immediate failure for recoverable issues.'}, {'name': 'Circuit Breaker', 'type': 'Reliability', 'implementation': 'Configured within Amazon API Gateway for its integrations with the backend services. The breaker will trip after a configured number of consecutive failures, preventing further calls for a timeout period.', 'applicability': "Protects the system from cascading failures. When a backend service (e.g., 'User & Data Service') is degraded or down, the circuit breaker prevents repeated, failing requests, allowing the service time to recover and ensuring the client fails fast."}, {'name': 'Cache-Aside', 'type': 'Performance', 'implementation': "The 'User & Data Service' will use an external cache (e.g., Redis) to store frequently accessed, slowly changing data, as suggested by the caching strategies in the database design. The application logic first checks the cache and, on a miss, queries the PostgreSQL DB and populates the cache.", 'applicability': 'Critical for meeting performance non-functional requirements like REQ-1-042 (API response time < 200ms) by reducing read latency and decreasing the load on the primary PostgreSQL database for operations like fetching user profiles or custom modes.'}, {'name': 'API Throttling', 'type': 'Scalability', 'implementation': "Rate limiting and request throttling rules are configured within Amazon API Gateway's usage plans.", 'applicability': 'A crucial security and reliability pattern that protects the backend services from being overwhelmed by excessive requests, whether from a single misbehaving client or a denial-of-service attack. Ensures fair usage and service stability.'}]

